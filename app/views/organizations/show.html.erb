<h1 class="page-organization-title"><%= @organization.name if @organization.status == 1 %> Organizations</h1>
<% @organization.each do |organization| %>
<div class="container col-md-4 home-recipient-wrapper">
    <%= render partial: "shared/organizations_img", locals: { organization: organization } %>
    <%= render partial: "shared/organizations_details", locals: { organization: organization } %>
  </div>
</div>
<% end %>
